{
  "name": "jq-wasm",
  "version": "0.2.2",
  "description": "Run JQ with WebAssembly",
  "scripts": {
    "docker:up": "docker compose up -d && docker compose exec emsdk bash",
    "docker:down": "docker compose down",
    "make": "docker compose run --rm emsdk make",
    "bump": "yarn upgrade --latest",
    "test": "yarn node --experimental-vm-modules $(yarn bin jest)"
  },
  "author": "Paolo Simone",
  "license": "ISC",
  "homepage": "https://github.com/paolosimone/jq-wasm#readme",
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
